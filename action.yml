name: 'Kolga Deploy'
description: 'This action is used to execute Kolga deploy step'
inputs:
  track:
    description: 'Track where to deploy eg. review, staging, stable'
    required: true
    default: 'review'
runs:
  using: 'docker'
  image: 'docker://ghcr.io/andersinno/kolga:212237601133c9911adac5289329a291523e167d-production'
  entrypoint: /app/devops
  args:
    - deploy_application
    - '--track'
    - ${{ inputs.track }}
