name: 'Kolga Deploy'
description: 'This action is used to execute Kolga deploy step'
inputs:
  track:
    description: 'Track where to deploy eg. review, staging, stable'
    required: true
    default: 'review'
runs:
  using: 'docker'
  image: 'docker://ghcr.io/andersinno/kolga:e3708a0aa7c1edd54c74778f2cadc7bed3b09108-production'
  entrypoint: /app/devops
  args:
    - deploy_application
    - '--track'
    - ${{ inputs.track }}
